node_modules
dist
dist-electron
release
.env
.env.local
*.sqlite
*.db
userData/
.DS_Store
.turbo
*.log

# Íconos/fondos binarios: AHORA se versionan (los necesita el build de CI).
# Antes estaban ignorados y "se regeneraban del SVG", pero la CI no corría
# generate:icons → faltaban icon.ico (Windows usaba ícono default) y
# dmg-background.png (el .dmg de macOS fallaba con ENOENT). Se commitean los
# binarios reales generados con scripts/generate-icons.mjs.
